<!DOCTYPE html>
<html>
<head>
    <title>Search Results</title>
</head>
<body>
    <h1>Search Results</h1>
    {{#if movies}}
        <ul>
            {{#each movies}}
                <li>{{this.Title}} ({{this.Year}})</li>
            {{/each}}
        </ul>
    {{else}}
        <p>No results found.</p>
    {{/if}}
</body>
</html>
